---
type: execute
mode: quick
autonomous: true
files_modified:
  - .planning/research/westlaw-lexisnexis-integration.md
---

<objective>
Retrieve the Westlaw/LexisNexis vendor documentation pages that a prior (network-sandboxed) research session could not reach, extract page-level verbatim technical detail (auth endpoints, request/response shapes, deep-link formats, API catalog contents), and write a single consolidated Markdown research report that upgrades every "medium confidence" claim from the prior session into CONFIRMED / REFUTED / STILL UNRESOLVED with cited evidence.

Purpose: openclerk-core's `westlawProvider.ts` and `lexisNexisProvider.ts` are currently speculative stubs built on guessed endpoint paths and a guessed OAuth2 shape in `EnterpriseCitationProvider`/`fetchClientCredentialsToken` (`src/providers/base.ts`). Before any redesign of that auth/lookup contract can be trusted, the guesses need to be replaced with page-level evidence — and the load-bearing safety question (can a vendor deep link or API ever *verify* a citation, or only *display* one?) needs a confirmed answer, because the hallucination-check feature's correctness depends on never letting a display-only link masquerade as a verification source.
Output: `.planning/research/westlaw-lexisnexis-integration.md` — a single research report. No files under `src/` are read for editing purposes beyond reference, and none are modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@src/providers/base.ts
@src/providers/westlawProvider.ts
@src/providers/lexisNexisProvider.ts
@src/providers/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrieve Thomson Reuters / Westlaw pages and record raw evidence</name>
  <files>.planning/research/westlaw-lexisnexis-integration.md</files>
  <action>
Create the `.planning/research/` directory and start `.planning/research/westlaw-lexisnexis-integration.md` with a title, a one-paragraph summary of the task (this is session 2 of this research; session 1 was network-blocked and produced search-snippet-level findings only), and a `## Thomson Reuters / Westlaw — Raw Findings` section.

For each of the following 7 URLs, attempt retrieval with WebFetch first. If WebFetch returns only a client-rendered shell (a SPA placeholder with no real body text — this is common on `developerportal.thomsonreuters.com` and `developers.thomsonreuters.com`, which are React-driven portals) or an error, fall back to the Browser tools: `mcp__Claude_Browser__navigate` to the URL, then `mcp__Claude_Browser__read_page` or `get_page_text` to extract the rendered DOM text. Do not attempt to bypass TLS verification, authentication, or any paywall/login gate — if a page requires sign-in to view, record that as the retrieval outcome, do not try to work around it.

URLs (labelled 1-5, with 4 split into three sub-fetches):
1. https://developerportal.thomsonreuters.com/authentication — the single most important page: exact token endpoint host/path, whether `audience`/`scope`/`grant_type` are required in the request body, token TTL, and the header format used to call APIs afterward.
2. https://developerportal.thomsonreuters.com/3e-api/getting_started/02---api-authentication-and-access — a concrete worked auth example likely shared across TR's APIs; use it to corroborate or contradict what page 1 says.
3. https://developerportal.thomsonreuters.com/westlaw-us-dockets-api — a real, named Westlaw API: capture its base URL, request path, query/body params, a response JSON shape if shown, rate limits, and any CORS statement (relevant because Word add-ins call from a browser context).
4a. https://developers.thomsonreuters.com/
4b. https://developers.thomsonreuters.com/pages/api-catalog/4EB79538-7677-4AF8-AC32-F73B26DBD473
4c. https://developers.thomsonreuters.com/pages/api-catalog/5A0B2E6E-DE81-42E6-9431-D78B8B4F0D35 — from these three, enumerate every legal/Westlaw-named API you can find in the catalog listing, by exact name. The goal is to confirm or refute that none of them retrieves case-law text/metadata by citation and none is a public KeyCite API.
5. https://legal.thomsonreuters.com/en/products/legal-api — TR's own marketing/framing of "legal API" access: how a firm is expected to request access, and whether it names any case-law-by-citation capability.

For each URL, append a subsection to the file in this exact structure so Task 3 can synthesize it later:
"### N. <short title> — <full URL>" followed by a "Fetch method:" line (WebFetch / Browser fallback / both attempted), a "Result:" line (Retrieved — verbatim excerpt below / Blocked or error — exact error text quoted verbatim), a "Verbatim technical detail:" block with short quoted excerpts (endpoints, param names, JSON snippets, rate limits) each attributed inline to this URL, and a "Relevant open question(s):" line naming which of the 6 open questions (see Task 3) this evidence bears on. Quote small excerpts only — do not paste entire pages. Never invent or infer a technical detail that the page did not state; if a claim from the prior session's search-snippet-level findings is neither confirmed nor contradicted by what you retrieved, say so explicitly rather than letting it stand unlabeled.
  </action>
  <verify>
    <automated>grep -q "developerportal.thomsonreuters.com/authentication" .planning/research/westlaw-lexisnexis-integration.md && grep -q "3e-api/getting_started" .planning/research/westlaw-lexisnexis-integration.md && grep -q "westlaw-us-dockets-api" .planning/research/westlaw-lexisnexis-integration.md && grep -q "developers.thomsonreuters.com" .planning/research/westlaw-lexisnexis-integration.md && grep -q "4EB79538-7677-4AF8-AC32-F73B26DBD473" .planning/research/westlaw-lexisnexis-integration.md && grep -q "5A0B2E6E-DE81-42E6-9431-D78B8B4F0D35" .planning/research/westlaw-lexisnexis-integration.md && grep -q "legal.thomsonreuters.com/en/products/legal-api" .planning/research/westlaw-lexisnexis-integration.md && echo OK</automated>
  </verify>
  <done>`.planning/research/westlaw-lexisnexis-integration.md` exists with a `## Thomson Reuters / Westlaw — Raw Findings` section containing one subsection per of the 7 TR URLs (items 1, 2, 3, 4a, 4b, 4c, 5), each with a Fetch method, Result, Verbatim technical detail (or exact error text if blocked), and Relevant open question(s) line.</done>
</task>

<task type="auto">
  <name>Task 2: Retrieve LexisNexis and deep-link/neutral-guide pages and record raw evidence</name>
  <files>.planning/research/westlaw-lexisnexis-integration.md</files>
  <read_first>.planning/research/westlaw-lexisnexis-integration.md</read_first>
  <action>
Read the file Task 1 produced, then append (do not overwrite) a `## LexisNexis — Raw Findings` section followed by a `## Deep-Link Formats & Neutral Court Guides — Raw Findings` section, using the same WebFetch-first-then-Browser-fallback strategy and the same TLS/auth/paywall boundary as Task 1 (read a public page; never sign in, never bypass a login wall — record it as blocked instead).

LexisNexis URLs (items 6-10, with 10 split into two sub-fetches):
6. https://dev.lexisnexis.com/gettingStarted — which APIs are documented, the auth model (look for `auth-api.lexisnexis.com` / `services-api.lexisnexis.com` hostnames, OAuth2 client-credentials, token path), and how a developer requests access.
7. https://www.lexisnexis.com/en-us/products/lexis-api.page — the catalog of Lexis APIs and what each covers; confirm or refute whether any covers case-law-by-citation retrieval or Shepard's.
8. https://www.lexisnexis.com/webserviceskit/v2_0beta/text/WSK-Welcome.htm — Web Services Kit's actual operations/content scope; confirm the news/Nexis-text-mining focus, and flag any legal/case-law operation and its auth mechanism if present.
9. https://www.lexisnexis.com/communities/academic/w/wiki/111.url-api-specifications.aspx — the Lexis URL API spec: the documented deep-link/permalink URL format and its parameters; whether a get-by-citation URL form exists (expected: no, only opaque permalink IDs — confirm or refute).
10a. https://help.lexisnexis.com/tabula-rasa/lmola/linktolacites_hdi-task?audience=litigation,transactional,pdf,pin-events,doctools,v4.2&lbu=US&locale=en_US
10b. https://supportcenter.lexisnexis.com/app/answers/answer_view/a_id/1089519/ — how Lexis's own tooling builds citation links; capture the exact URL shape it produces.

Deep-link / neutral court-guide URLs (items 11-12, with 12 split into two sub-fetches):
11. https://www.ned.uscourts.gov/internetDocs/cmecf/AttorneyGuide-Hyperlinking.pdf — verbatim URL templates for Westlaw/Lexis citation hyperlinks used in real federal court filings, including exact parameter names (e.g. `find/default.wl?cite=`, `Link/Document/FullText?findType=Y&cite=`).
12a. https://research.lib.buffalo.edu/creating-permanent-links/westlaw
12b. https://libguides.law.umn.edu/c.php?g=125778&p=823446 — confirm the current Westlaw/Lexis stable-link formats and whether sign-in is required to resolve them.

Use the identical per-URL subsection structure Task 1 established ("### N. <short title> — <full URL>", Fetch method, Result, Verbatim technical detail, Relevant open question(s)) for every URL in this task, so all 16 fetched sources share one consistent evidence format for Task 3 to synthesize. Quote small excerpts only, attribute each to its URL, and never invent a detail a page did not state.
  </action>
  <verify>
    <automated>grep -q "dev.lexisnexis.com/gettingStarted" .planning/research/westlaw-lexisnexis-integration.md && grep -q "lexis-api.page" .planning/research/westlaw-lexisnexis-integration.md && grep -q "webserviceskit/v2_0beta" .planning/research/westlaw-lexisnexis-integration.md && grep -q "111.url-api-specifications" .planning/research/westlaw-lexisnexis-integration.md && grep -q "linktolacites_hdi-task" .planning/research/westlaw-lexisnexis-integration.md && grep -q "a_id/1089519" .planning/research/westlaw-lexisnexis-integration.md && grep -q "AttorneyGuide-Hyperlinking.pdf" .planning/research/westlaw-lexisnexis-integration.md && grep -q "creating-permanent-links/westlaw" .planning/research/westlaw-lexisnexis-integration.md && grep -q "libguides.law.umn.edu" .planning/research/westlaw-lexisnexis-integration.md && echo OK</automated>
  </verify>
  <done>`.planning/research/westlaw-lexisnexis-integration.md` now also contains a `## LexisNexis — Raw Findings` section (items 6, 7, 8, 9, 10a, 10b) and a `## Deep-Link Formats & Neutral Court Guides — Raw Findings` section (items 11, 12a, 12b), each subsection following the same Fetch method / Result / Verbatim technical detail / Relevant open question(s) structure as Task 1, and Task 1's TR section is unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesize raw findings into the final research report</name>
  <files>.planning/research/westlaw-lexisnexis-integration.md</files>
  <read_first>.planning/research/westlaw-lexisnexis-integration.md</read_first>
  <action>
Read the full draft (Tasks 1 and 2's raw-findings sections), keep those sections in the file as a cited evidence appendix (rename to `## Appendix: Raw Source Evidence` if that reads better, but do not delete them — they are the traceability record for every claim made above them), and add the following sections above the appendix, in this order:

`## Executive Summary` — 3-5 sentences: what changed since the prior (blocked) session, and the headline conclusion on whether either vendor now looks buildable as a real (non-stub) provider.

`## Open Questions — Resolved` — one subsection per each of these 6 questions, each explicitly labelled CONFIRMED, REFUTED, or STILL UNRESOLVED (and if STILL UNRESOLVED because a page could not be retrieved even via Browser fallback, append "portal-gated" and quote the exact fetch/browser error encountered), with the answer stated plainly and a citation back to the specific appendix item(s):
1. TR token request shape — exact host, path, required body params (`audience`? `scope`?). Does a redesigned `EnterpriseCitationProvider` need a separate `tokenUrl` field and an `audience` field, distinct from the API base URL?
2. TR tenancy — is the API base URL global (one URL for all TR customers) or per-customer (firm-specific subdomain/path)?
3. Does ANY TR API return a case document or verify a case by citation? State the full enumerated catalog list found in Task 1 and explicitly conclude yes/no.
4. Lexis auth shape — confirm the actual token host/path, grant type, and whether case-law content is reachable via any documented API at all.
5. Deep-link get-by-citation — is there a URL constructable directly from a citation string for either vendor? State the exact template for each vendor if one exists (Westlaw's `find/default.wl?cite=` is expected to exist; Lexis's permalink is expected to require an opaque, non-derivable ID — confirm or refute both).
6. Verification vs. linking — does anything found let a deep link or API call distinguish a real citation from a fabricated one programmatically, without a signed-in human? State the conclusion explicitly, because this is the load-bearing reason `hallucinationCheck.ts` must keep using CourtListener (or another verification-capable provider) and must never treat a Westlaw/Lexis link as a verification signal.

`## Redesign Proposal: EnterpriseCitationProvider / fetchClientCredentialsToken` — a spec-only proposal (no code) for `src/providers/base.ts`: the real field set a firm-supplied credentials object needs for TR and for Lexis respectively (e.g. a separate token/auth URL distinct from the API base URL, an `audience` field if TR requires one, a `scope` field if either vendor requires one), and the real request shape (body params, headers, grant type) each vendor's token endpoint expects, per the confirmed evidence above — explicitly note anywhere the current stub's shape (single base URL + `/oauth/token` suffix, no audience) is now known to be wrong.

`## Providers: Ready Now vs. Configurable Shells` — for CourtListener, Westlaw, and Lexis, state plainly which can be made real today from public documentation alone versus which must stay a configurable shell awaiting a design-partner firm's actual credentials and a live endpoint to validate against, and why. Describe the shape of a type-enforced "link-only, never feeds the hallucination check" boundary — e.g. a distinct return type or interface tag on any provider whose `lookupCitation` can only ever produce a display URL and must be structurally prevented from being passed into `checkHallucination`'s verification path.

`## Caveats: Portal-Gated or Unobtainable` — list every URL from the 16 fetched (across Tasks 1 and 2) that could not be retrieved even with the Browser fallback, each with the exact error text encountered (HTTP status, auth-wall message, or tool error) — do not summarize or paraphrase the error, quote it.

Do not modify or create any file outside `.planning/research/westlaw-lexisnexis-integration.md`. Confirm no other files changed before finishing.
  </action>
  <verify>
    <automated>grep -q "## Executive Summary" .planning/research/westlaw-lexisnexis-integration.md && grep -q "## Open Questions" .planning/research/westlaw-lexisnexis-integration.md && grep -q "## Redesign Proposal" .planning/research/westlaw-lexisnexis-integration.md && grep -q "EnterpriseCitationProvider" .planning/research/westlaw-lexisnexis-integration.md && grep -q "fetchClientCredentialsToken" .planning/research/westlaw-lexisnexis-integration.md && grep -q "## Providers: Ready Now" .planning/research/westlaw-lexisnexis-integration.md && grep -q "## Caveats" .planning/research/westlaw-lexisnexis-integration.md && [ "$(grep -Ec 'CONFIRMED|REFUTED|STILL UNRESOLVED' .planning/research/westlaw-lexisnexis-integration.md)" -ge 6 ] && [ -z "$(git status --porcelain -- src/ 2>/dev/null)" ] && echo OK</automated>
  </verify>
  <done>The report has an Executive Summary, all 6 open questions individually resolved as CONFIRMED/REFUTED/STILL UNRESOLVED with citations back to appendix evidence, a spec-only Redesign Proposal for `EnterpriseCitationProvider`/`fetchClientCredentialsToken` naming the real TR and Lexis field sets, a Ready-Now-vs-Configurable-Shell classification per provider including the link-only/never-verifies boundary description, a Caveats section quoting exact errors for anything unobtainable, and `git status --porcelain -- src/` is empty (no `src/` files were touched).</done>
</task>

</tasks>

<verification>
1. `.planning/research/westlaw-lexisnexis-integration.md` exists and contains all 16 individually cited source URLs (7 TR + 9 Lexis/deep-link) across its raw-evidence appendix.
2. Every one of the 6 open questions from the prior session has an explicit CONFIRMED / REFUTED / STILL UNRESOLVED status with cited evidence, not left as an open guess.
3. The Redesign Proposal section is spec-only prose (field names, endpoint shapes, request params) — contains no TypeScript code blocks and proposes no change to any file under `src/`.
4. `git status --porcelain -- src/` and `git status --porcelain -- tests/` are both empty — this task touched only `.planning/research/`.
5. Any URL that could not be retrieved (portal-gated, login-walled, or tool error even after WebFetch + Browser fallback) is explicitly listed in the Caveats section with its exact error text, not silently omitted.
</verification>

<success_criteria>
- A single, self-contained Markdown research report exists at `.planning/research/westlaw-lexisnexis-integration.md` that a future planner can read to redesign `EnterpriseCitationProvider`/`fetchClientCredentialsToken` and the Westlaw/Lexis providers without re-doing this research.
- Every claim in the report's Executive Summary and Open Questions sections is traceable to a quoted excerpt and source URL in the appendix — no claim rests on the prior session's unconfirmed search snippets.
- The report states plainly, with evidence, whether any vendor path exists today for programmatically verifying a citation (versus only linking to one) — this directly gates whether `hallucinationCheck.ts` may ever treat a Westlaw/Lexis result as a verification signal.
- No file under `src/` or `tests/` was modified; the only file this plan creates or changes is `.planning/research/westlaw-lexisnexis-integration.md`.
</success_criteria>

<output>
Create `.planning/quick/260721-18r-retrieve-blocked-vendor-pages-extract-we/260721-18r-SUMMARY.md` when done.
</output>